version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    restart: unless-stopped

  # --- phase 2: persistence ---
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: fibrion
  #     POSTGRES_USER: fibrion
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

  # --- phase 3: dashboard ---
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend

# volumes:
#   pgdata: